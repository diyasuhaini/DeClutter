import { QtyFilterPipe } from './qty-filter.pipe';

describe('QtyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QtyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
