import { FilterReportPipe } from './filter-report.pipe';

describe('FilterReportPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportPipe();
    expect(pipe).toBeTruthy();
  });
});
